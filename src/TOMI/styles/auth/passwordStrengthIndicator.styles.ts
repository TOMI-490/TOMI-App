import { StyleSheet } from 'react-native';

export const passwordStrengthIndicatorStyles = StyleSheet.create({
  container: {
    marginTop: 8,
    marginBottom: 4,
  },
  label: {
    fontSize: 12,
    fontWeight: '500',
    marginTop: 4,
  },
});
