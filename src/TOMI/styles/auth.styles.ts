import { StyleSheet } from 'react-native';

export const authStyles = StyleSheet.create({
  formContainer: {
    marginBottom: 24,
  },
  bottomSection: {
    alignItems: 'center',
    paddingTop: 16,
  },
  hintText: {
    fontSize: 14,
    color: '#666',
    marginBottom: 12,
    textAlign: 'center',
  },
  headerSection: {
    marginBottom: 32,
    alignItems: 'flex-start',
  },
  title: {
    fontSize: 28,
    fontWeight: '700',
    color: '#1a1a1a',
    marginBottom: 4,
  },
  subtitle: {
    fontSize: 15,
    color: '#666',
    fontWeight: '400',
  },
});
